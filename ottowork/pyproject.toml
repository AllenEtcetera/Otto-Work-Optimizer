# This project was generated with 0.3.20 using template: https://github.com/beeware/briefcase-template@v0.3.20
[tool.briefcase]
project_name = "Otto Work Optimizer"
bundle = "com.example"
version = "0.0.1"
url = "https://github.com/AllenEtcetera/Otto-Work-Optimizer"
license.file = "LICENSE"
author = "Allen Rutledge"
author_email = "allencr1998@gmail.com"

[tool.briefcase.app.ottowork]
formal_name = "Otto Work Optimizer"
description = "Computer productivity tool."
long_description = """More details about the app should go here.
"""
sources = [
    "src/ottowork",
]
test_sources = [
    "tests",
]

console_app = true
requires = [
]
test_requires = [
    "pytest",
]

[tool.briefcase.app.ottowork.macOS]
universal_build = true
requires = [
]

[tool.briefcase.app.ottowork.linux]
requires = [
]

[tool.briefcase.app.ottowork.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ottowork.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ottowork.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ottowork.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ottowork.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "24.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.ottowork.windows]
requires = [
]

# Mobile deployments
[tool.briefcase.app.ottowork.iOS]
supported = false

[tool.briefcase.app.ottowork.android]
supported = false

# Web deployments
[tool.briefcase.app.ottowork.web]
supported = false

